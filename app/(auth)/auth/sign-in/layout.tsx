export const metadata = {
  title: "Sign In | Pixn",
  description: "Sign in to your account to view your gallery.",
  icons: {
    icon: "/favicon.svg",
  },
};

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default Layout;
